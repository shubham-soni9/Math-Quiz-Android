<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sudoku">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.sudoku.ui.SudokuMainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.sudoku.ui.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.sudoku.ui.SudokuMainActivity" />

        <activity
            android:name="com.sudoku.ui.GameActivity"
            android:label="@string/title_activity_game_view"
            android:theme="@style/AppTheme.NoActionBar" />


        <activity android:name="com.sudoku.ui.LoadGameActivity" />

        <activity
            android:name="com.sudoku.ui.StatsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name="com.sudoku.ui.HelpActivity"
            android:label="@string/title_activity_help" />

        <service
            android:name="com.sudoku.controller.GeneratorService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>